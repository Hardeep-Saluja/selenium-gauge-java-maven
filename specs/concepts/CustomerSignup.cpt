# Sign up a new customer with first name as <fname>, last name as <lname> with <password>
* Sign up using <fname> and <lname> with <password>
* Display a message "Thank you for signing up! You are now logged in."